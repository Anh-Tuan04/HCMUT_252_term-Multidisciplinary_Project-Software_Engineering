name:
services:
  db:
    container_name:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE:
      MYSQL_USER:
      MYSQL_PASSWORD:
      MYSQL_ROOT_PASSWORD:
    ports:
      - '****:3306'
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: